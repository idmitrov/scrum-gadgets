/**
 * @name validationConstants
 * @desc All validation constants
 */
export const validationConstants = {
  register: {
    passwordDoesNotMatch: 'Password and confirm password does not match'
  }
};
